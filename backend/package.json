{
  "name": "auditor-edge",
  "version": "1.0.0",
  "type": "module",
  "description": "Edge API for macOS notch app - R2 uploads, D1, Vectorize, Durable Objects",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "wrangler dev --ip 127.0.0.1 --port 8787",
    "deploy": "wrangler deploy",
    "whoami": "wrangler whoami",
    "login:windows": "wrangler login --callback-host 127.0.0.1 --callback-port 8977",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate:local": "wrangler d1 execute auditor --local --file=./migrations/001_init.sql && wrangler d1 execute auditor --local --file=./migrations/002_jobs.sql",
    "migrate:prod": "wrangler d1 execute auditor --remote --file=./migrations/001_init.sql && wrangler d1 execute auditor --remote --file=./migrations/002_jobs.sql",
    "type-check": "tsc --noEmit"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "edge",
    "durable-objects",
    "r2",
    "d1"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3",
    "vitest": "4.1.2",
    "wrangler": "4.78.0"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.2.0",
    "zod": "^3.22.4"
  }
}
